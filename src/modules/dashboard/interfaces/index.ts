export interface IPropsCategoryItem {
  imageUri: string;
  title: string;
  total: number;
}

export interface IPropsUIItem {
  category: string;
  imageUri: string;
  linkTo: string;
  title: string;
}
